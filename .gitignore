// This file is used to ignore files and directories in the repository

.DS_Store
.env
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
build

style/output.css

#ignore generated go components code
components/**/*.go
#ignore only the generated layout code, need views.go files to render them with echo.
views/layout_templ.go

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
vendor/

# Go workspace file
go.work

# node modules
node_modules

# air build files
tmp

# task file temporary files
.task
